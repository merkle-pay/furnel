# Node environment
NODE_ENV=development

# PostgreSQL
POSTGRES_PASSWORD=your_secure_password_here

# Domain (use :80 for local dev, or your-domain.com for production)
DOMAIN=:80

# Caddy Basic Auth (generate with: make hash)
CADDY_ADMIN_USER=admin
CADDY_ADMIN_HASH=$2a$14$your_bcrypt_hash_here

# Coinbase Developer Platform (CDP)
CDP_PROJECT_ID=
CDP_API_KEY_ID=
CDP_API_KEY_NAME=
CDP_API_KEY_PRIVATE_KEY=
# Webhook ID (from CDP Portal > Webhooks > your webhook UUID)
# Used to verify webhook signatures
COINBASE_WEBHOOK_ID=

# Solana
SOLANA_RPC=https://api.devnet.solana.com
USDC_MINT=EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v

# MoonPay test deposit address (Sepolia ETH)
# Set this to detect MoonPay sandbox purchases (sends ETH on Sepolia testnet)
# When set, the system will poll Sepolia for incoming ETH instead of Solana USDC
MOONPAY_DEPOSIT_ADDRESS=

# Furnel deposit address (your Solana wallet with real USDC)
# Used for Coinbase offramp - this is where your real USDC sits
FURNEL_DEPOSIT_ADDRESS=

# Callback domain for Coinbase redirect (use ngrok/cloudflare tunnel for testing)
# In production, this should be your actual domain
CALLBACK_DOMAIN=http://localhost

# Frontend URL (for redirects after offramp)
FRONTEND_URL=http://localhost:3001
REDIRECT_URL=http://localhost/api/webhooks/coinbase/callback

# MoonPay
# Publishable key (frontend widget)
VITE_MOONPAY_API_KEY=pk_test_your_publishable_key
# Secret key (server-side URL signing)
MOONPAY_SECRET_KEY=sk_test_your_secret_key
# Webhook signing key (wk_test_... from dashboard - verify webhook signatures)
MOONPAY_WEBHOOK_SIGNING_KEY=wk_test_your_webhook_signing_key

# Email (Resend - https://resend.com)
# Used to send Coinbase offramp link to recipient
RESEND_API_KEY=re_your_resend_api_key
EMAIL_FROM=Furnel <noreply@your-domain.com>
